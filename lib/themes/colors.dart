import 'package:flutter/material.dart';

const primary = Color(0xFFFF5722); // สีส้ม (Primary Color)
const primaryDark = Color(0xFFE64A19); // สีส้มแดงเข้ม (Dark Primary Color)
const primaryLight = Color(0xFFFFCCBC); // สีส้มอ่อนมาก (Light Primary Color)
const primaryText = Color(0xFF212121); // สีดำ (Primary Text)
const secondaryText = Color(0xFF757575); // สีเทาเข้ม (Secondary Text)
const accent = Color(0xFFFF5252); // สีแดงสด (Accent Color)
const icons = Color(0xFFFFFFFF); // สีขาว (Text/Icons)
const divider = Color(0xFFBDBDBD); // สีเทาอ่อน (Divider Color)
const star = Color(0xFFFFD700); // สีเหลือง (Yellow)
